# Bagat la build-time (npm run build) — trebuie sa fie IDENTIC cu RIS_API_KEY
# din .env (root, backend). Fara el, daca RIS_API_KEY e setat pe backend,
# frontend-ul construit primeste 401 pe orice apel /api/*.
VITE_RIS_API_KEY=
