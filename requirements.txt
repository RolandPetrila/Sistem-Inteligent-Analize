# Core
# CRITICA #3 (audit 2026-07-13): fastapi 0.115.5 plafona starlette la 0.41.3 (8 avize
# pip-audit, incl. CVE leak NTLM via StaticFiles pe Windows — serviciul ruleaza ca SYSTEM).
# fastapi 0.139 cere starlette>=0.46.0 FARA plafon superior -> pinam explicit starlette la
# versiunea care repara TOATE avizele (PYSEC-2026-249 = 1.3.1, nu doar cele din 1.1.0).
# Pereche oficial suportata; verificat: 443 pytest + pip check + job real DONE + multipart/WS.
fastapi==0.139.0
starlette==1.3.1
uvicorn[standard]==0.32.0
pydantic==2.12.5
pydantic-settings==2.13.1
# python-multipart: cerut de FastAPI pt upload-uri form (OCR /api/documents/ocr, batch CSV,
# companies/import) — era FOLOSIT dar NEDECLARAT (finding audit suplimentar). 0.0.22 avea
# 5 CVE; 0.0.31 le repara pe toate.
python-multipart==0.0.31

# Database
aiosqlite==0.22.1

# AI / LLM
langgraph==1.1.3

# HTTP / Web
httpx==0.28.1

# Report Generation
# CRITICA #2 (audit 2026-07-13): fpdf2 2.9.x NU exista pe PyPI (ultima reala = 2.8.7) —
# floor-ul 2.9.0 facea orice instalare fresh (inclusiv pip-audit) sa esueze. Verificat cu
# `pip index versions fpdf2`. Floor coborat la 2.8.0 (2.8.7 e cea instalata + testata).
fpdf2>=2.8.0,<3.0.0
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2

# Notifications
aiosmtplib==5.1.0

# Monitoring
prometheus-client>=0.21.0

# Utilities
tiktoken>=0.7.0
loguru==0.7.2
networkx>=3.0
zeep==4.3.3
requests==2.32.5
aiofiles>=23.2.1
# F23: pin transitive urllib3 floor (CVE PYSEC-2026-141/142) — fresh installs can't float to a vulnerable version
urllib3>=2.5.0
